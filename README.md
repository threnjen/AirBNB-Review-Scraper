# AirBNB-Review-Scraper
A simple scraper that takes review data from AirBNB listings within a zip code to find the common pros and cons of listings in the area.

Use `config.json` to set up your parameters.
Run `pipenv shell` to load the ENV vars.
Run the script with `python main.py`
